name: Deploy to Production
on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout le code
        uses: actions/checkout@v4

      - name: Lancer l'application
        run: |
          cd /chemin/de/votre/projet/CESI-ZEN-TP
          git pull
          docker compose up -d --build